    <div class="ui large inverted menu">
      <div class="ui container">
        <a class="active item" href="<%= root_path %>">
          ChatRoom
        </a>
        <a class="item">
          Messages
        </a>
        <div class="right menu">
          <div class="ui dropdown item">
            Account <i class="dropdown icon"></i>
            <div class="menu">
              <%if !logged_in? %>
              <a class="item" href="<%= login_path %>">Log in </a>
              <% else %>
                <a class="item" href="<%= logout_path %>">Log out</a>              
              <% end %>
            </div>
          </div>
          <%if !logged_in? %>
            <div class="item">
                <div class="ui primary button"><a class="signupbtn" href="<%= signup_path %>">Sign Up</a></div>
            </div>
          <%end%>
        </div>
      </div>
    </div>